# 链表
## 1.建立链表
```c
#include  <stdlib.h>
struct node{   
        int   data;
        struct node *link;
} ;
typedef struct node *Nodeptr;
typedef struct node Node; 
Nodeptr list, p;

Nodeptr createList1( int n )  /*创建一个具有n个结点的链表 */
{
   /* list是链表头指针, q指向新申请的结点，p指向最后一个结点*/
    Nodeptr  p, q, list=NULL;
　  int i;
    for(i=0;i<n;i++)
    {
        q=(Nodeptr)malloc(sizeof(Node));
        q->data=read();     	/* 取一个数据元素 */ 
        q->link=NULL; 
        if (list==NULL) 	/*链表为空*/
            list=p=q;
        else
            p->link=q;       /* 将新结点链接在链表尾部 */
        p=q;
    }
    return list;
}

Nodeptr createList2()//哑节点链表
{
    Nodeptr  p, q, list, head;
    int n,i,a;
	scanf("%d",&n);
	scanf("%d",&a);
	
	q=(Nodeptr)malloc(sizeof(Node));
	list=p=q;
	
	q=(Nodeptr)malloc(sizeof(Node));
	q->data=a;
    q->link=NULL;
	p->link=q;
	head=p=q;

    for(i=2;i<=n;i++)
    {
        scanf("%d",&a);
        q=(Nodeptr)malloc(sizeof(Node));
		q->data=a;
		p->link=q;
		p=q;
    }

    return list;
}

Nodeptr creatList3()    //空链表
{
    Node* list = malloc(sizeof(node));
    list->next = NULL;
    return list;
}

```
## 2.输出链表
```c
void printList(Nodeptr p)
{
	p = p->link;//跳过哑节点
    while(p != NULL){
        printf("%d %d ",p->coefficient,p->index);
        p = p->link;
    }
}
```


## 3.求线性链表的长度
```c
int getLength( Nodeptr list )
{
    Nodeptr p;     /* p为遍历链表结点的指针 */ 
    int n=0;                  /* 链表的长度置初值0 */ 
    for(p=list; p!=NULL; p=p->link)/* p依次指向链表的下一结点 */                    
        n++;    	　　    /* 对链表结点累计计数 */   
    return n;                   /* 返回链表的长度n */ 
}
```

## 4.查找
```c
//寻找第k个节点
    Nodeptr  p,q,r,list=NULL;
    r=p;
	p=list;//初始化r为尾节点，p为头节点 
    for(p=list,i=1;i<=k-1;i++)
    {
    	r=p;
		p=p->link;
	}
```


## 5.插入数据
```c
//在非空线性链表的第一个结点前插入一个数据项为item的新结点
Nodeptr insertFirst( Nodeptr list, ElemType item )
{
    /* list指向链表第一个链结点 */ 
    p=(Nodeptr)malloc(sizeof(Node)); 
    p->data=item;           /* 将item赋给新结点数据域 */
    p->link=list;       /* 将新结点指向原链表第一个结点*/
    return  p;            /* 将链表新头指针返回 */ 
}
list = insertFirst(list, item);//主函数使用

//在线性链表中由指针p指的链结点之后插入一个数据项为item 的链结点
void  insertNode1(Nodeptr p, ElemType item)
{    
    Nodeptr q;
    q=(Nodeptr)malloc(sizeof(Node));
    q->data=item;                 /* 将item送新结点数据域 */
    q->link = NULL;
    q->link=p->link;
    p->link=q;
}

//在线性链表中第n(n>0)个结点后面插入一个数据项为item的新结点
void  insertNode2( Nodeptr list, int n, ElemType item )
{
    Nodeptr p=list, q;
    int i;
    for(i=1;i<=n-1;i++)    /* 寻找第i个结点 */
    { 
        if(p->link==NULL)
            break;                 /* 不存在第i个结点 */
        p=p->link;
    }   
    q=(Nodeptr)malloc(sizeof(Node));
    q->data=item;        /* 将item送新结点数据域 */
    q->link = NULL;
    q->link=p->link;
    p->link=q;              /* 将新结点插入到第i个结点之后 */
}

//在有序线性链表中相应位置上插入一个数据项为item的新结点
Nodeptr  insertNode3(Nodeptr list, ElemType elem)
{ 
    Nodeptr p,q, r;
    r = (Nodeptr)malloc(sizeof(Node)); //创建一个数据项为elem的新结点
    r->elem = elem;   r->link = NULL;
    if(list == NULL) 	 /* list是一个空表 */
        return r;
    for(p=list; elem > p->elem && p != NULL;  q = p, p = p->link) /* 找到插入位置 */
        ;
    if( p == list){ /* 在头结点前插入 */
        r->link = p;
        return r;
    } 
    else { /* 在结点q后插入一个结点 */
        q->link = r;
        r->link = p;
    }
    return list;
}
list = insertNode3(list, item);//主函数使用
```
## 6.删除数据
```c
//从非空线性链表中删除p指向的链结点,有前驱r
Nodeptr deleteNode1( Nodeptr list, Nodeptr r,  Nodeptr p )
{
    if(p==list)  
     list=p->link;         /* 删除链表的第一个链结点*/
    else
        r->link=p->link;  /* 删除p指的链结点*/
    free(p);                        /* 释放被删除的结点空间*/
    return list
}

//从非空线性链表中删除p指向的链结点,无前驱r
Nodeptr deleteNode2( Nodeptr list, Nodeptr p )
{
    Nodeptr r;
    if(p==list){                         /*当删除链表第一个结点*/
        list=list->link;
        free(p);                        /*释放被删除结点的空间*/
    }
    else{
        for(r=list; r->link!=p  &&  r->link!=NULL; r=r->link); /*移向下一个链结点*/
            if(r->link!=NULL){ 
                r->link=p->link;
                free(p);                  
            }
    } 
    return list;
}

//从非空线性链表中删除包含给定元素的结点
Nodeptr deleteNode( Nodeptr list, ElemType elem )
{
    Nodeptr p, q;    /*p指向要删除的结点，q为p的前一个结点*/
    for(p=list; p!=NULL; q=p,p=p->link) 
        if(p->elem==elem ）         /*找到要删除的链结点*/
            break;
    if(p==list) {                         /* 删除头结点*/
        list = list->link;
        free(p);                  
    } 
    if(q->link != NULL) {            /* 删除p指向的结点*/
        q->link = p->link;
        free(p);
    }
    return list;
}
```



